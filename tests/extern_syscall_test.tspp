// Integration test for external function calls
extern function tspp_sys_write(fd: int, buf: string, count: int): int;

function main(): int {
    // Test syscall through external function
    tspp_sys_write(1, "Hello from TSPP extern!\n", 25);
    return 0;
}