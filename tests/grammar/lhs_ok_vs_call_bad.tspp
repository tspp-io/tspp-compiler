// EXPECT_COMPILE_ERROR: invalid assignment target

function f(x: int): int { return x; }

function main(): int {
  let y: int = 0;
  // Bad: call on LHS
  f(1) = y;
  // Good: property assignment
  let holder: int*; // avoid null deref; do nothing at runtime
  return 0;
}
