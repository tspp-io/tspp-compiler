// Allocation-heavy microbenchmark
function main(): int {
  let n: int = 100000;
  let sum: int = 0;
  for (let i: int = 0; i < n; i = i + 1) {
    let arr: []int = memory.alloc(10); // using stdlib memory.alloc
    arr[0] = i;
    sum = sum + arr[0];
    // GC handles free
  }
  return sum;
}
